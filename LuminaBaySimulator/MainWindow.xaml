<Window x:Class="LuminaBaySimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LuminaBaySimulator"
        mc:Ignorable="d"
        Title="Lumina Bay Simulator - Phase 2" Height="650" Width="1000"
        Background="#121212">

    <d:Window.DataContext>
        <local:MainViewModel/>
    </d:Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F1F1F" Padding="15,10" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding WorldTime.FullDateString}" Foreground="#FFD700" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding WorldTime.LocalizedPhase}" Foreground="#B0BEC5" FontSize="18"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#99000000" CornerRadius="20" 
                        HorizontalAlignment="Center" VerticalAlignment="Center" Padding="25,8"
                        Visibility="{Binding IsStatusVisible, Converter={StaticResource BoolToVis}}" IsHitTestVisible="False">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#4FC3F7" FontWeight="SemiBold" FontSize="14"/>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="#2E7D32" CornerRadius="8" Padding="12,5" Margin="0,0,15,0">
                        <TextBlock Text="{Binding Player.Money, StringFormat='{}€ {0}'}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                    </Border>
                    <StackPanel Width="120" VerticalAlignment="Center">
                        <Grid Margin="0,2">
                            <ProgressBar Value="{Binding Player.Energy}" Height="10" Foreground="#76FF03" Background="#333" BorderThickness="0"/>
                            <TextBlock Text="Energia" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <ProgressBar Value="{Binding Player.Stress}" Height="10" Foreground="#FF3D00" Background="#333" BorderThickness="0"/>
                            <TextBlock Text="Stress" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" ClipToBounds="True">

            <Grid Visibility="{Binding CurrentViewMode, Converter={local:EnumToVisConverter}, ConverterParameter=Map}">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding AvailableLocations}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center" Margin="50"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.TravelToLocationCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" Margin="15" BorderThickness="0" Background="Transparent">
                                    <Border Width="220" Height="150" CornerRadius="12" ClipToBounds="True">
                                        <Grid>
                                            <Image Source="{Binding ImagePath}" Stretch="UniformToFill"/>
                                            <Border Background="#99000000" VerticalAlignment="Bottom" Padding="10">
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Grid Visibility="{Binding CurrentViewMode, Converter={local:EnumToVisConverter}, ConverterParameter=LocationInside}">

                <Image Source="{Binding CurrentLocation.ImagePath}" Stretch="UniformToFill" Opacity="0.7"/>

                <ItemsControl ItemsSource="{Binding NpcsInLocation}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding CurrentX}"/>
                            <Setter Property="Canvas.Top" Value="{Binding CurrentY}"/>
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.InteractWithNpcCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Grid>
                                            <Image Source="{Binding SpritePath}" Height="250" Stretch="Uniform" ToolTip="{Binding Name}">
                                                <Image.Effect>
                                                    <DropShadowEffect ShadowDepth="0" BlurRadius="15" Color="White" Opacity="0.4"/>
                                                </Image.Effect>
                                            </Image>

                                            <Border Background="#CC000000" VerticalAlignment="Bottom" HorizontalAlignment="Center" CornerRadius="4" Padding="5,2" Margin="0,0,0,10">
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </Grid>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20">
                    <Button Command="{Binding BackToMapCommand}" Content="⬅ Mappa" Padding="10,5" Background="#333" Foreground="White" Margin="0,0,0,10"/>
                    <Button Command="{Binding OpenShopCommand}" Content="🛒 Negozio" Padding="10,5" Background="#E65100" Foreground="White"/>
                </StackPanel>
            </Grid>

            <Grid Background="#CC000000" Visibility="{Binding CurrentViewMode, Converter={local:EnumToVisConverter}, ConverterParameter=Dialogue}">
                <Border Background="White" VerticalAlignment="Bottom" Height="250" Margin="50,0,50,30" CornerRadius="15" BorderBrush="Black" BorderThickness="2">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
                    </Border.Effect>

                    <Grid Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="{Binding CurrentNpc.SpritePath}" Stretch="Uniform" VerticalAlignment="Bottom" Margin="0,0,20,0"/>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{Binding CurrentNpc.Name}" FontSize="22" FontWeight="Bold" Foreground="#E65100" Margin="0,0,0,10"/>

                            <TextBlock Grid.Row="1" Text="{Binding CurrentDialogueNode.Text}" FontSize="18" TextWrapping="Wrap" Margin="0,0,0,15" Foreground="#333"/>

                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding CurrentDialogueNode.Choices}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding Text}" 
                                        Command="{Binding DataContext.SelectChoiceCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding}"
                                        Margin="0,4" Padding="15,8" 
                                        Background="#F5F5F5" BorderBrush="#CCC" 
                                        HorizontalContentAlignment="Left" FontSize="16" Cursor="Hand">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="5"/>
                                                    </Style>
                                                </Button.Resources>
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#FFECB3"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Grid Background="#FFF3E0" Visibility="{Binding CurrentViewMode, Converter={local:EnumToVisConverter}, ConverterParameter=Shop}">
                <Button Command="{Binding CloseShopCommand}" Content="Chiudi" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20"/>
            </Grid>

            <ItemsControl ItemsSource="{Binding FloatingEffects}" IsHitTestVisible="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding StartX}"/>
                        <Setter Property="Canvas.Top" Value="{Binding StartY}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" Foreground="{Binding Color}" FontSize="24" FontWeight="Bold" RenderTransformOrigin="0.5,0.5">
                            <TextBlock.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </TextBlock.RenderTransform>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)"
                                                                     From="0" To="-100" Duration="0:0:2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="1" To="0" Duration="0:0:2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Grid>

        <Border Grid.Row="2" Background="#212121" Padding="5">
        </Border>

    </Grid>
</Window>